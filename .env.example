# Supabase (public values, safe to expose in the browser; the table is protected
# by row-level security). These defaults are already baked into the app, so env
# vars are OPTIONAL and only needed if you rotate keys or move projects.
NEXT_PUBLIC_SUPABASE_URL=https://dngrtzjupoezzyhdguvz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_ONC6Q9Hde32eNDbe-Z0XqA_5Itw0N2m
